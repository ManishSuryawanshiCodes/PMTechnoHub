# Images

| File | Use |
|------|-----|
| `logo.png` | Navbar, hero, favicon |
| `pcb-traces-bg.svg` | Site-wide trace background |
| `gallery/*.jpg` | Project covers & albums (`js/data.js`) |

Add gallery JPEGs listed in `data.js` → `galleryProjects[].images`.
